#include <stdio.h>

/**
 * main - prints the alphabet in lowercase
 * 
 * Returns: returns 0
 */
int main(void)
{
	int ch;
        printf(“Print all lowercase letters o the next line :\n”); 
	for (ch= ‘a’; ch<= ‘z’; ch++)
		putchar(ch);
			putchar (‘\n’) ;
	return(0);
}
