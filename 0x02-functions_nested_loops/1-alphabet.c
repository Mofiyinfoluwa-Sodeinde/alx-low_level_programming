#include "main.h"

/**
 * print_alphabet - prints the alphabet in lowercase
 *
 * Return: 0
 */
void print_alphabet(void)
{
	char qe;

	for (qe = 'a'; qe <= 'z'; qe++)
		_putchar(qe);

	_putchar('\n');
}
